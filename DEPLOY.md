# 部署说明

这是一个**纯静态 PWA**，不需要数据库、不需要后端，适合 3 个人共享同一个网址。

## 最快部署方式

### 方式 A：Cloudflare Pages
1. 打开 Cloudflare Pages。
2. 新建项目并上传本目录。
3. 不需要构建命令。
4. 输出目录使用项目根目录。
5. 部署完成后会得到 `https://xxxxx.pages.dev`。
6. 把网址发给另外两位同行者。

### 方式 B：GitHub Pages
1. 新建一个 GitHub repository。
2. 上传本目录全部文件。
3. Settings → Pages。
4. Source 选择 GitHub Actions 或从 main 分支部署。
5. 得到一个公开网址。

### 方式 C：Netlify
把本目录拖到 Netlify 的部署页面即可获得公网网址。

## 手机使用
打开网址后：
- iPhone：Safari → 分享 → 添加到主屏幕
- Android：Chrome → 添加到主屏幕

这样它会像一个旅行 App 一样打开。

## 三个人一起用
三个人访问**同一个网址**即可：
- 行程内容是共享的
- “分享行程”可直接发送链接
- 每个人的打勾清单保存在自己的手机/浏览器里，不会互相覆盖
- 不需要登录

## 地图
景点/酒店的“📍导航”按钮使用 Google Maps 搜索链接。实际导航前请以当天地图、道路、营业时间和活动方通知为准。

## 后续升级
如果需要三个人的“实时共同勾选、备注、上传照片、实时修改行程”，就需要增加在线数据库和登录/访问控制；当前版本刻意保持无后端，部署最简单、成本最低。
